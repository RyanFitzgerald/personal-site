import styled from 'styled-components';
import { headingColour } from '../../variables';

const H3 = styled.h3`
  color: ${headingColour};
  font-size: 1.2em;
  margin: 0 0 7px;
`;

export default H3;