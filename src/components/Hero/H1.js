import styled from 'styled-components';
import { headingColour } from '../../variables';

const H1 = styled.h1`
  color: ${headingColour}
  font-size: 3.2em;
  font-weight: 400;
  margin: 0 0 0 -3px;
`;

export default H1;